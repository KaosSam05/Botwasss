const welcome = (number, groupname) => {
    return `Hola @${number}. Sea Bienvenido a ${groupname}!!`
}
exports.welcome = welcome

const bye = (number) => {
    return `Adiós @${number}. Hasta Luego Vuelve Pronto 👋`
}
exports.bye = bye
